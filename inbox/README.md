# Inbox

Drop exported artist JSON files here before running `scripts/Sincronizar_Pavoneo.bat`.

The sync script validates privacy, copies each JSON to `data/artistas/`, commits, and can optionally push.
